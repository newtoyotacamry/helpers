return function(trigger)
    if not trigger or trigger.__NoxTrigger ~= true then
        warn("🚫 Unauthorized access attempt detected.")
        warn("❌ This script can only be run through the official loader button.")
        return "__unauthorized__"
    end

    warn("✅ Protected script loaded and executed via secure button.")
    -- Insert your actual game logic here
end
