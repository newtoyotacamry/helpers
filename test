return function(trigger)
	if not trigger or trigger.__NoxTrigger ~= true then
		warn("🚫 Unauthorized access attempt to ProtectedScript")
		return
	end

	print("✅ Protected script loaded and executed via button!")
	-- secure functionality here
end
